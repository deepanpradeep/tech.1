name: leap year

on:
 push:
  branch: -main

job:
  run_on: ubuntu_latest

step: 
  -name: setup python
     use: version 3.8

  -name: install dependencies
     run: pip install -r requirements.txt

  -name: run leap yearcheck
     run:  python leap_year.py

 

